meta {
  name: Delete Dataset
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/api/catalog?id=dataset-001
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}

assert {
  res.status: lt 500
}
