meta {
  name: Participant Credentials
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/participants/{{participantId}}/credentials
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}
