meta {
  name: List Tenants
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/admin/tenants
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}

assert {
  res.status: lt 500
}

docs {
  CFM tenant inventory (admin only).
}
