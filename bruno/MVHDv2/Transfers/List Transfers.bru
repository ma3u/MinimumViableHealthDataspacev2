meta {
  name: List Transfers
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/transfers?participantId={{participantId}}
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}

assert {
  res.status: lt 500
}
