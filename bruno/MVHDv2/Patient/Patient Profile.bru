meta {
  name: Patient Profile
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/patient/profile?patientId={{patientId}}
  body: none
  auth: inherit
}

headers {
  Accept: application/json
}

assert {
  res.status: lt 500
}

docs {
  Single patient profile — encounters, conditions, medications, observations.
}
