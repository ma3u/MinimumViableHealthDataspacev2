# Opt-in overlay: EDC v0.18-aligned JAD launchers (issue #97 Phase B).
#
# Consumes Metaform's public per-commit images instead of our 2026-04-11
# ghcr.io/ma3u builds — pinned to jad main HEAD 4a7e5bd0 (2026-05-18), which
# builds against edc = 0.18.0-SNAPSHOT. Analysis + validation gate:
# docs/planning/current/issue-97-phase-b-analysis.md
#
# Usage (testing only — NOT the default stack):
#   docker compose -f docker-compose.yml -f docker-compose.jad.yml \
#     -f docker-compose.jad-edc018.yml up -d
#
# Promote to docker-compose.jad.yml + scripts/azure/env.sh only after the
# validation gate passes (DCP + DSP TCK + EHDS suites, seed phases 1-7,
# Playwright live project).

services:
  controlplane:
    image: ghcr.io/metaform/jad/controlplane:4a7e5bd096c58814748e956cc9329586ad309698

  dataplane-fhir:
    image: ghcr.io/metaform/jad/dataplane:4a7e5bd096c58814748e956cc9329586ad309698

  dataplane-omop:
    image: ghcr.io/metaform/jad/dataplane:4a7e5bd096c58814748e956cc9329586ad309698

  identityhub:
    image: ghcr.io/metaform/jad/identity-hub:4a7e5bd096c58814748e956cc9329586ad309698

  issuerservice:
    image: ghcr.io/metaform/jad/issuerservice:4a7e5bd096c58814748e956cc9329586ad309698
